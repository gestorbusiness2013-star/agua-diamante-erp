rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regla global: Requiere autenticación para cualquier lectura/escritura
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
